[build]
  command = "npm install"  # Mantenha se necessário, ou mude para "npm run build" se tiver script
  functions = "netlify/functions"  # OK, define pasta das funções

[functions.sincronizar_bling]  # Correto: Subseção para função específica (sem [[ ]])
  background = true  # Ativa modo background (evita timeout de 10s)

# Remova o plugin se não for essencial; teste sem ele primeiro
# [[plugins]]
#   package = "@netlify/plugin-functions-install-core"
